/**
 * @author Ichen.chu
 * created on 01.03.2018
 */
(function () {
    'use strict';

    angular.module('BlurAdmin.pages.myInput', [
    ])
        .config(routeConfig);

    /** @ngInject */
    function routeConfig($stateProvider) {
    }
})();
